# Personalization Notes

Put your optional OpenClaw personalization files in this directory.

Examples:
- custom markdown docs
- alternate `openclaw.json`
- local `skills/`, `tools/`, `agents/` content
